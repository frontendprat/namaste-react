----------- Lecture 1 ------------------------------------------------------------------------------------------------

# What is CDN?

# What is crossorigin?

# The most expensive work for browser is when the DOM nodes need manipulation

# Git & Gthub is different

---

-------------- Lecture 2-----------------------------------------------------------------------------------------------------

# npm: It doesn't stand for node package manager

- It does manage packagess though.

# parcel, vite, parcel (Bundlers)

- Bundlers are tools that take your code and assets and bundle them together into a single file (or a few files) that can be served to the browser. They also handle things like transpiling, minifying, and optimizing your code.

# Dev dependencies vs dependencies

- Dev dependencies are packages that are only needed during development, such as testing frameworks or build tools. Dependencies are packages that are required for the application to run in production.

# ^, ~, and \* in package.json

- ^ (caret): Allows updates that do not change the left-most non-zero digit in the version (e.g., ^1.2.3 allows 1.2.4 but not 1.3.0).
- ~ (tilde): Allows updates that do not change the left-most non-zero digit in the version, but only for the last digit (e.g., ~1.2.3 allows 1.2.4 but not 1.3.0).
- - (asterisk): Allows any version to be installed, regardless of the version number.

# package-lock.json vs package.json

- package.json is a file that lists the dependencies of your project and their versions. It is used by npm to install the correct versions of packages when you run npm install.
- package-lock.json is a file that is automatically generated by npm when you install packages. It locks the exact versions of the dependencies and their dependencies, ensuring that the same versions are installed every time you run npm install.

# node_modules folder

- node_modules fetches all the code for all the dependencies listed in package.json and package-lock.json. It is created when you run npm install and contains all the code for the packages that your project depends on.
- Stores transitive dependencies (dependencies of dependencies) as well. It is important to note that the node_modules folder can become quite large, so it is often excluded from version control systems like Git.

# Whatever you can re-generate, don't put it on git.

# type="module" in <script> tag

- The type="module" attribute in a <script> tag tells the browser that the script is a JavaScript module. This allows you to use import and export statements in your code, which can help you organize your code into smaller, reusable pieces. Because normal browser scripts cannot use import/export, you need to use type="module" to enable this functionality.

# parcel

- Dev build
- local server
- HMR (Hot Module Replacement): It allows you to see changes in your code without having to refresh the entire page. This can speed up development and make it easier to test changes.
- File Watching Algorithm: written in c++.
- Faster builds - Caching
- Image optimization
- Minification
- Bundling
- Compression
- Consistent hashing: <---- read about this
- Code splitting
- Differential bundling - gives support for older browsers by creating separate bundles for modern and legacy browsers.
- Diagnostics
- HTTPs
- Tree shaking: It is a technique used to remove unused code from your final bundle. This can help reduce the size of your code and improve performance.
- diff dev & prod bundles

-------------------------- Lecture 3-----------------------------------------------------------------------------------------------------

# To run the code in a project just check the package.json file for the scripts section. You can run the code by using the command `npm run <script-name>` in the terminal.

# React.createElement create an object, it does not create a HTML element. The object is a representation of the HTML element that will be created when it is rendered.

# JSX

- JSX is not a part of React. JSX is seperate and React is seperate.
- JSX is not html in javascript. It is html like syntax in javascript.

# Who do you write code for? Machine or humans?

- First humans, then machine. Write code for humans to read and understand, then make it work for the machine.
- Because if we were wirting for machine we would write in binary.

# Jsx in background is React.createElement. And who is doing this work? Babel is doing this work. Babel is a transpiler that converts JSX code into JavaScript code that can be understood by the browser.

# If you have to give attributes to a JSX element, you have to wrtie it in camelCase. For example, instead of writing `class` you would write `className`.

# If your jsx is multiline, you have to wrap it in parentheses. ()

# Inside JSX, you can use JavaScript expressions by wrapping them in curly braces {}. For example, you can use a variable or a function call inside JSX by wrapping it in curly braces.

# Ways of Calling functional components:

- {title()}
- <Title />
- <Title></Title>

-------------------------- Lecture 4-----------------------------------------------------------------------------------------------------

# props

- props is the short form for properties.
- just normal arguments to a function.

# config driven UI

- UI is driven by the config.
- Config-driven UI is a pattern where the structure, content, and behavior of your UI is defined by data (a config object/JSON), rather than being hardcoded directly into your components.

# .map()

- whenever you are using .map() function, you need to give a unique key to each element in the list. This helps React identify which items have changed, are added, or are removed, and it improves the performance of your application. Or else React will throw a warning in the console. And will re render the entire list instead of just the changed items. This helps in optimizing the performance of your application.

- It is not recommended to use index as a key because it can lead to unexpected behavior when the list changes. Instead, use a unique identifier for each item in the list, such as an ID or a unique property of the item.
